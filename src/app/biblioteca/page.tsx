import { Library } from "@/components/thirtyx/Library";

export const dynamic = "force-dynamic";

/** Historial de la diseñadora: pedidos eliminados del tablero + entregados. */
export default function BibliotecaPage() {
  return <Library />;
}
